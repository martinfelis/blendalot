# SCsub

Import('env')

module_env = env.Clone()

module_env.Append(CPPDEFINES=["BLENDALOT_MODULE"])

blendalot_sources = [
  "register_types.cpp",
  "src/example_class.cpp"
  ]

module_env.add_source_files(env.modules_sources, blendalot_sources) # Add all cpp files to the build

if env["tests"]:
    module_env.Append(CPPDEFINES=["TESTS_ENABLED"])
    # module_env.add_source_files(env.modules_sources, "./tests/*.cpp")